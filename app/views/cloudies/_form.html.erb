    <%= form_with model: @cloudy, local: true do |f| %>

   <div class="field">
     <%= f.label :image, "プロトタイプの画像" %>
      <%= f.file_field :image %>
   </div>
      <%#= form.text_area :text, placeholder: "text", rows: "10" %>
      <%= f.submit "SEND" %>
    <% end %>
